# Personal-Budget-App

To run the backend, try running flask run if that doesn't work then run main.py

To run the frontend, cd into frontend folder, and run the following command: npm run dev

testing

testing 2